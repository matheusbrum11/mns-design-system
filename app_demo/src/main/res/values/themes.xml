<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Tema Android mínimo. Toda a aparência real vem do MnsTheme em Compose;
      este style existe apenas para a janela não piscar branco no cold start.
    -->
    <style name="Theme.MnsDemo" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
